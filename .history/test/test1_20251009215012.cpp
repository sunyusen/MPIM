#include <bits/stdc++.h>
using namespace std;

int f(int n, int m){
	vector<int> arr(100000, -1);
	arr[n] = 0;
	queue<int> que;
	que.push(n);
	while(!que.empty()){
		int now = que.front();
		que.pop();
		if(arr[now+1] == -1){{ arr}
	}
}