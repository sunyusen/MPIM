#include<bit/stdc++.h>
using namespace std;

int min_change(vector<int>& arr){
	int n = arr.size();
	int m = arr.size()/2;
	//首先初始化cost数组
	vector<vector<int>> cost(m, vector<int>(10,0));
	for(int i = 0; i < m; i++){
		int a = arr[i*2], b = arr[i*2+1];
		for(int j = 1; j < 10; j++){
			cost[i][j] = ()
		}
	}

	return res;
}
int main(){

}