#include<bit/stdc++.h>
using namespace std;

int min_change(vector<int>& arr){
	int n = arr.size();
	//首先初始化cost数组
	vector<vector<int>> cost(n, vector<int>(10,0));
	for(int i = 0; i < )

	return res;
}
int main(){

}